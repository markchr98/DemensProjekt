@model IEnumerable<DemensProjekt.Models.Post>

@{
    ViewBag.Title = "Se og lav Indlæg";
    Layout = "_Layout";
}

<h1>Explorerer's Blog</h1>

<p>
    Below are a few of the latest posts from some of our explorerers
</p>

<p class="pager">
    @if (ViewBag.HasPreviousPage)
    {
        <a class="prev" href="@Url.Action("Index", "Blog", new { page = ViewBag.PreviousPage })">
            &lt; Previous
        </a>
    }

    @if (ViewBag.HasNextPage)
    {
        <a class="next" href="@Url.Action("Index", "Blog", new { page = ViewBag.NextPage })">
            Next &gt;
        </a>
    }
</p>

<div class="blog-posts">
    @foreach (var post in Model)
    {
        @Html.Partial("_Post", post)
    }
</div>